@{
    Layout = null;
    var title = ViewData["Title"] ?? "NT.WEB";
    // Product cards data is prepared in HomeController.Index and passed via ViewBag.ClientLayoutProducts.
}
@inject NT.WEB.Services.ProductWebService ProductService
@inject NT.WEB.Services.ProductDetailWebService ProductDetailService
@inject NT.WEB.Services.BrandWebService BrandService
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@title - NT.WEB</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @RenderSection("Styles", required: false)

    <style>
        html, body { height: 100%; }
        body { background: linear-gradient(135deg, #f5f7fb 0%, #e8f4f8 100%); display: flex; flex-direction: column; min-height: 100vh; }
        
        /* Header Styles */
        .topbar { 
            background: linear-gradient(135deg, #00838f 0%, #00bcd4 100%); 
        border-bottom: none;
       box-shadow: 0 2px 20px rgba(0, 0, 0, 0.15);
       position: sticky;
          top: 0;
       z-index: 1000;
  }
        
      .site-logo { 
            color: #fff; 
  font-weight: 700; 
   display: flex; 
        align-items: center; 
gap: .5rem;
        transition: transform 0.3s ease;
      }
        
        .site-logo:hover {
  transform: scale(1.02);
        }
        
 .site-logo img { 
     background: transparent; 
      display: block; 
         height: 60px; 
       width: auto;
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
       border-radius: 8px;
       padding: 4px;
       background: linear-gradient(135deg, #00838f 0%, #00bcd4 100%);
}
        
   .site-logo span {
        color: #ffd700;
  background: none;
        -webkit-background-clip: unset;
  -webkit-text-fill-color: #ffd700;
        background-clip: unset;
        font-size: 1.3rem;
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
    }
        
        /* Navigation Links */
  .navbar-nav .nav-link {
  font-weight: 500;
   color: #fff;
      padding: 0.5rem 1rem;
         border-radius: 8px;
         transition: all 0.3s ease;
        }
        
     .navbar-nav .nav-link:hover {
 background: rgba(255, 255, 255, 0.2);
            color: #fff;
        }

        /* Search Box */
 .search-wrapper {
     position: relative;
     flex-grow: 1;
     max-width: 500px;
 }
        
 .search-form {
     display: flex;
     align-items: center;
     background: #f5f5f5;
     border-radius: 30px;
     padding: 4px;
     transition: all 0.3s ease;
     border: 2px solid transparent;
 }
        
 .search-form:focus-within {
     background: white;
     border-color: #00bcd4;
     box-shadow: 0 0 0 3px rgba(0, 188, 212, 0.15);
 }
        
 #client-search {
     border: none;
     background: transparent;
     padding: 8px 16px;
     font-size: 0.9rem;
     flex-grow: 1;
     outline: none;
 }
        
 #client-search::placeholder {
     color: #90a4ae;
 }
        
 .search-btn {
     background: linear-gradient(135deg, #00838f, #00bcd4);
     border: none;
     color: white;
     width: 40px;
     height: 40px;
     border-radius: 50%;
     display: flex;
     align-items: center;
     justify-content: center;
     cursor: pointer;
     transition: all 0.3s ease;
     flex-shrink: 0;
 }
        
 .search-btn:hover {
     transform: scale(1.05);
     box-shadow: 0 4px 15px rgba(0, 131, 143, 0.4);
 }
        
 #client-suggest {
     position: absolute;
     top: calc(100% + 8px);
     left: 0;
     right: 0;
     background: white;
     border-radius: 16px;
     box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
     max-height: 400px;
     overflow-y: auto;
     z-index: 1050;
     display: none;
 }
        
 #client-suggest .suggest-item {
     display: flex;
     align-items: center;
     padding: 12px 16px;
     text-decoration: none;
     color: #263238;
     transition: all 0.2s ease;
     border-bottom: 1px solid #f0f0f0;
 }
        
 #client-suggest .suggest-item:last-child {
     border-bottom: none;
 }
        
 #client-suggest .suggest-item:hover {
     background: #e0f7fa;
 }
        
 #client-suggest .suggest-img {
     width: 50px;
     height: 50px;
     object-fit: cover;
     border-radius: 10px;
     margin-right: 12px;
 }
        
 #client-suggest .suggest-info h6 {
     font-weight: 600;
     font-size: 0.9rem;
     margin-bottom: 2px;
     color: #263238;
 }
        
 #client-suggest .suggest-info .suggest-price {
     font-size: 0.85rem;
     color: #00838f;
     font-weight: 600;
 }
        
        /* Footer Styles */
        .footer-cta { 
     background: linear-gradient(135deg, #00838f 0%, #00bcd4 100%); 
      color: #fff; 
            padding: 3rem 1rem;
        }
    
        .footer-cta h5, .footer-cta h6 {
            font-weight: 700;
       margin-bottom: 1rem;
        }
    
        .footer-cta a {
      transition: all 0.3s ease;
        }
        
        .footer-cta a:hover {
       color: #b2ebf2 !important;
        padding-left: 5px;
        }
        
  main { flex: 1 0 auto; }
        footer { margin-top: auto; }
   
        /* Product Card Styles */
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1.5rem;
   padding: 0.5rem;
        }

        .product-card {
 background: white;
        border-radius: 16px;
   overflow: hidden;
box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
         transition: all 0.3s ease;
      border: none;
      }
        
        .product-card:hover {
       transform: translateY(-8px);
     box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
        }
        
        .product-card .card-img-wrapper {
            position: relative;
            overflow: hidden;
    background: linear-gradient(135deg, #f5f5f5, #fafafa);
        }
    
        .product-card .card-img-top {
          height: 180px;
       object-fit: contain;
            padding: 15px;
            transition: transform 0.4s ease;
      }
        
        .product-card:hover .card-img-top {
            transform: scale(1.08);
        }
        
        .product-card .card-body {
        padding: 1rem 1.2rem;
        }
        
        .product-card .product-name {
         font-weight: 600;
            color: #263238;
            font-size: 0.95rem;
          line-height: 1.4;
       margin-bottom: 0.5rem;
        display: -webkit-box;
   -webkit-line-clamp: 2;
       -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
      .product-card .product-name:hover {
  color: #00838f;
        }
        
    .product-card .product-brand {
 color: #78909c;
            font-size: 0.85rem;
            margin-bottom: 0.5rem;
     }
        
        .product-card .product-price {
            font-weight: 700;
     font-size: 1.1rem;
      background: linear-gradient(135deg, #00838f, #00bcd4);
   -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
   background-clip: text;
        }
        
        .product-card .quick-view {
            position: absolute;
            bottom: -50px;
 left: 50%;
      transform: translateX(-50%);
     background: linear-gradient(135deg, #00838f, #00bcd4);
   color: white;
            padding: 8px 20px;
            border-radius: 20px;
        font-size: 0.85rem;
            font-weight: 500;
    transition: all 0.3s ease;
        opacity: 0;
    }
        
        .product-card:hover .quick-view {
            bottom: 15px;
            opacity: 1;
   }
    </style>
</head>
@using System.Security.Claims
@{
    var isAuth = User?.Identity?.IsAuthenticated ?? false;
    var userId = isAuth ? User?.FindFirst(ClaimTypes.NameIdentifier)?.Value : null;
    var role = isAuth ? User?.FindFirst(ClaimTypes.Role)?.Value : null;
    var fullName = isAuth ? User?.FindFirst("Fullname")?.Value : null;
    var userName = isAuth ? User?.FindFirst(ClaimTypes.Name)?.Value : null;
}
<body data-auth="@isAuth" data-userid="@userId" data-role="@role">
    <header class="topbar">
        <nav class="navbar navbar-expand-lg container">
            <a class="navbar-brand site-logo" asp-controller="Home" asp-action="Index">
                <img src="~/css/logo (1).png" alt="Logo" />
                <span>NT Fishing Shop</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="d-flex align-items-center w-100">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Index">Trang chủ</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="GetAllProducts">Sản phẩm</a></li>
                       @*  <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Privacy">Chính sách</a></li> *@
                    </ul>

                    <div class="search-wrapper mx-auto">
                        <form class="search-form" role="search" method="get" asp-controller="Home" asp-action="GetAllProducts" autocomplete="off">
                            <i class="fas fa-search ms-3 text-muted"></i>
                            <input id="client-search" type="search" name="q" placeholder="Tìm kiếm sản phẩm..." />
                            <button class="search-btn" type="submit" title="Tìm kiếm">
                                <i class="fas fa-arrow-right"></i>
                            </button>
                        </form>
                        <div id="client-suggest"></div>
                    </div>

                    <div class="d-flex align-items-center ms-auto">
                        @{
                            var errorMsg = TempData["Error"] as string;
                            var hasAnyMsg = !string.IsNullOrWhiteSpace(errorMsg);
                        }
                        <div class="dropdown me-3">
                            <button class="btn btn-sm btn-outline-secondary position-relative" type="button" data-bs-toggle="dropdown" aria-expanded="false" title="Thông báo">
                                <i class="fa fa-bell"></i>
                                @if (hasAnyMsg)
                                {
                                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">!</span>
                                }
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end p-0" style="min-width: 320px;">
                                <li class="p-2 border-bottom fw-semibold">Thông báo lỗi</li>
                                @if (hasAnyMsg)
                                {
                                    <li><div class="px-3 py-2 small text-danger">@errorMsg</div></li>
                                }
                                else
                                {
                                    <li><div class="px-3 py-2 small text-muted">Không có lỗi</div></li>
                                }
                            </ul>
                        </div>
                      @*   <a class="me-3 text-muted" href="#"><i class="fa fa-heart"></i></a> *@
                      @{ 
                          var isAdminOrEmpTop = string.Equals(role, "Admin", StringComparison.OrdinalIgnoreCase) || string.Equals(role, "Employee", StringComparison.OrdinalIgnoreCase);
                          var isCustomer = isAuth && string.Equals(role, "Customer", StringComparison.OrdinalIgnoreCase);
                      }
                      @if (isCustomer)
                      {
                          @await Component.InvokeAsync("CartSummary")
                      }
                        @if (isAuth)
                        {
                            var isAdminOrEmp = string.Equals(role, "Admin", StringComparison.OrdinalIgnoreCase) || string.Equals(role, "Employee", StringComparison.OrdinalIgnoreCase);
                            <span class="nav-link text-muted small me-2">Xin chào: @(string.IsNullOrWhiteSpace(fullName) ? (string.IsNullOrWhiteSpace(userName) ? userId : userName) : fullName)</span>
                            @if (isAdminOrEmp)
                            {
                                <a class="nav-link position-relative" asp-controller="Orders" asp-action="Index">Xác nhận đơn hàng @await Component.InvokeAsync("OrdersPendingCount")</a>
                                <span class="mx-2 text-muted">|</span>
                                <a class="nav-link" asp-controller="Admin" asp-action="Index"> Quản lý</a>
                            }
                            else
                            {
                                <a class="nav-link" asp-controller="Orders" asp-action="My">Đơn hàng của tôi</a>
                            }
                            <span class="mx-2 text-muted">|</span>
                            <a class="nav-link" asp-controller="Account" asp-action="Profile"><i class="fas fa-user-cog me-1"></i>Tài khoản</a>
                            <span class="mx-2 text-muted">|</span>
                            <a class="nav-link" asp-controller="Account" asp-action="Logout">Đăng xuất</a>
                        }
                        else
                        {
                            <a class="nav-link" asp-controller="Account" asp-action="RegisterCustomer" asp-route-client="true">Đăng ký</a>
                            <span class="mx-2 text-muted">|</span>
                            <a class="nav-link" asp-controller="Account" asp-action="Login" asp-route-client="true">Đăng nhập</a>
                        }
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <main class="container my-4">
        @RenderBody()
        @{
        var products = ViewBag.ClientLayoutProducts as IEnumerable<dynamic>;
        }
        @if (products != null && products.Any())
        {
          <div class="product-grid mb-4">
     @foreach (var p in products)
    {
             <div class="product-card">
  <div class="card-img-wrapper">
          <a asp-controller="Product" asp-action="ProductDetailIndex" asp-route-id="@p.Id" class="text-decoration-none">
<img src="@((p.Thumbnail ?? "/images/product-placeholder.png"))" class="card-img-top" alt="@p.Name" />
      </a>
   <a asp-controller="Product" asp-action="ProductDetailIndex" asp-route-id="@p.Id" class="quick-view text-decoration-none">
   <i class="fas fa-eye me-1"></i> Xem chi tiết
      </a>
           </div>
      <div class="card-body">
     <a asp-controller="Product" asp-action="ProductDetailIndex" asp-route-id="@p.Id" class="text-decoration-none">
              <div class="product-name">@p.Name</div>
      </a>
              <div class="product-brand"><i class="fas fa-tag me-1"></i>@p.BrandName</div>
<div class="product-price">@((p.Price != null) ? string.Format(System.Globalization.CultureInfo.GetCultureInfo("vi-VN"), "{0:C0}", p.Price) : "Liên hệ")</div>
        </div>
          </div>
     }
    </div>
        }
    </main>

    <footer>
        <div class="footer-cta">
            <div class="container">
                <div class="row">
                    <div class="col-md-4">
                        <h5>Chào mừng</h5>
                        <p>Mua sắm đồ câu tốt nhất. Miễn phí vận chuyển với đơn trên 2.000.000đ.</p>
                    </div>
                    <div class="col-md-2">
                        <h6>Tag Clouds</h6>
                        <ul class="list-unstyled small">
                            <li>Rods</li>
                            <li>Reels</li>
                            <li>Lines</li>
                        </ul>
                    </div>
                    <div class="col-md-3">
                        <h6>Liên kết chính</h6>
                        <ul class="list-unstyled small">
                            <li><a class="text-white" asp-controller="Home" asp-action="Index">Trang chủ</a></li>
                            <li><a class="text-white" asp-controller="Product" asp-action="Index">Sản phẩm</a></li>
                        </ul>
                    </div>
                    <div class="col-md-3">
                        <h6>Liên kết hữu ích</h6>
                        <ul class="list-unstyled small">
                            <li><a class="text-white" asp-controller="Home" asp-action="Privacy">Chính sách</a></li>
                            <li><a class="text-white" asp-controller="Home" asp-action="Contact">Liên hệ</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="bg-dark text-light py-2">
            <div class="container d-flex justify-content-between small">
                <div>&copy; @DateTime.UtcNow.Year - NT.WEB</div>
                <div>Developed by Your Team</div>
            </div>
        </div>
    </footer>

    <!-- Support Chat Box -->
    <div id="support-chatbox" class="support-chatbox">
        <div class="chatbox-header" onclick="toggleChatbox()">
            <i class="fas fa-headset"></i>
            <span>Hỗ trợ khách hàng</span>
            <i class="fas fa-chevron-down chatbox-toggle-icon"></i>
        </div>
        <div class="chatbox-body">
            <div class="chatbox-content">
                <div class="qr-section">
                    <p class="qr-title"><i class="fas fa-qrcode me-2"></i>Quét QR để được hỗ trợ</p>
                    <img src="~/uploads/qr-code/qr.jpg" alt="QR Code Hỗ trợ" class="qr-image" />
                </div>
                <div class="hotline-section">
                    <p class="hotline-title"><i class="fas fa-phone-alt me-2"></i>Hotline hỗ trợ</p>
                    <a href="tel:0383177504" class="hotline-btn">
                        <i class="fas fa-phone-volume me-2"></i>
                        <span>0383 177 504</span>
                    </a>
                    <p class="hotline-note">Bấm để gọi trực tiếp</p>
                </div>
            </div>
        </div>
    </div>

    <style>
        /* Support Chatbox Styles */
        .support-chatbox {
            position: fixed;
            bottom: 20px;
            left: 20px; /* moved to left */
            right: auto;
            width: 320px;
            background: white;
            border-radius: 16px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
            z-index: 9999;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .support-chatbox.collapsed .chatbox-body {
            max-height: 0;
            padding: 0;
            opacity: 0;
        }

        .support-chatbox.collapsed .chatbox-toggle-icon {
            transform: rotate(180deg);
        }

        .chatbox-header {
            background: linear-gradient(135deg, #00838f 0%, #00bcd4 100%);
            color: white;
            padding: 15px 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .chatbox-header:hover {
            background: linear-gradient(135deg, #006064 0%, #00acc1 100%);
        }

        .chatbox-header span {
            flex-grow: 1;
        }

        .chatbox-toggle-icon {
            transition: transform 0.3s ease;
        }

        .chatbox-body {
            max-height: 400px;
            padding: 20px;
            opacity: 1;
            transition: all 0.3s ease;
            overflow: hidden;
        }

        .chatbox-content {
            text-align: center;
        }

        .qr-section {
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid #e0e0e0;
        }

        .qr-title {
            color: #00838f;
            font-weight: 600;
            margin-bottom: 15px;
            font-size: 0.95rem;
        }

        .qr-image {
            width: 180px;
            height: 180px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            border: 3px solid #e0f7fa;
        }

        .hotline-section {
            text-align: center;
        }

        .hotline-title {
            color: #00838f;
            font-weight: 600;
            margin-bottom: 15px;
            font-size: 0.95rem;
        }

        .hotline-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #4caf50 0%, #81c784 100%);
            color: white;
            padding: 12px 24px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 700;
            font-size: 1.2rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(76, 175, 80, 0.3);
        }

        .hotline-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 20px rgba(76, 175, 80, 0.4);
            color: white;
        }

        .hotline-btn i {
            animation: phoneRing 1s ease-in-out infinite;
        }

        @@keyframes phoneRing {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(-10deg); }
            50% { transform: rotate(10deg); }
            75% { transform: rotate(-5deg); }
        }

        .hotline-note {
            color: #78909c;
            font-size: 0.85rem;
            margin-top: 10px;
            margin-bottom: 0;
        }

        /* Floating button when chatbox is fully collapsed/hidden */
        .support-float-btn {
            position: fixed;
            bottom: 20px;
            left: 20px; /* moved to left */
            right: auto;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #00838f 0%, #00bcd4 100%);
            border-radius: 50%;
            display: none;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
            box-shadow: 0 4px 20px rgba(0, 131, 143, 0.4);
            z-index: 9998;
            transition: all 0.3s ease;
            animation: pulse 2s infinite;
        }

        .support-float-btn:hover {
            transform: scale(1.1);
        }

        @@keyframes pulse {
            0% { box-shadow: 0 4px 20px rgba(0, 131, 143, 0.4); }
            50% { box-shadow: 0 4px 30px rgba(0, 131, 143, 0.6), 0 0 0 10px rgba(0, 188, 212, 0.1); }
            100% { box-shadow: 0 4px 20px rgba(0, 131, 143, 0.4); }
        }

        /* Mobile responsive */
        @@media (max-width: 576px) {
            .support-chatbox {
                width: calc(100% - 40px);
                right: 20px;
                left: 20px;
            }
            .support-float-btn { left: 20px; right: auto; }
        }
    </style>

    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.4/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
        // Toggle Support Chatbox
        function toggleChatbox() {
            const chatbox = document.getElementById('support-chatbox');
            if (chatbox) {
                chatbox.classList.toggle('collapsed');
            }
        }

        // Initialize chatbox as collapsed on page load
        document.addEventListener('DOMContentLoaded', function() {
            const chatbox = document.getElementById('support-chatbox');
            if (chatbox) {
                chatbox.classList.add('collapsed');
            }
        });

        (function(){
            const input = document.getElementById('client-search');
            const box = document.getElementById('client-suggest');
            if(!input || !box) return;
            let timer = null;
            const currency = n => { try { return new Intl.NumberFormat('vi-VN',{style:'currency',currency:'VND',maximumFractionDigits:0}).format(n); } catch { return n; } };
            const hide = () => { box.style.display='none'; box.innerHTML=''; };
            const show = items => {
                if(!items || items.length===0){ hide(); return; }
                box.innerHTML = items.map(i => {
                    const stockBadge = i.isInStock 
                        ? '' 
                        : '<span class="badge bg-danger ms-2" style="font-size: 0.7rem;">Hết hàng</span>';
                    const priceDisplay = i.isInStock 
                        ? `<span class="suggest-price">${i.priceMin ? currency(i.priceMin) : 'Liên hệ'}${i.priceMax && i.priceMin !== i.priceMax ? ' - ' + currency(i.priceMax) : ''}</span>`
                        : '<span class="suggest-price text-danger">Tạm hết hàng</span>';
                    return `
                    <a href="/Product/ProductDetailIndex/${i.id}" class="suggest-item">
                        <img src="${i.thumbnail || '/images/product-placeholder.png'}" class="suggest-img" alt="thumb" style="${!i.isInStock ? 'opacity: 0.6;' : ''}"/>
                        <div class="suggest-info">
                            <h6>${i.name}${stockBadge}</h6>
                            ${priceDisplay}
                        </div>
                    </a>`;
                }).join('');
                box.style.display='block';
            };
            input.addEventListener('input', function(){
                const q = this.value.trim();
                if(timer) clearTimeout(timer);
                if(q.length < 1){ hide(); return; }
                timer = setTimeout(async ()=>{
                    try{
                        const res = await fetch(`/Product/Suggest?q=${encodeURIComponent(q)}`);
                        if(!res.ok){ hide(); return; }
                        const data = await res.json();
                        show(data);
                    }catch{ hide(); }
                }, 250);
            });
            document.addEventListener('click', e => { if(!box.contains(e.target) && e.target !== input) hide(); });
        })();

        // Simple client-side 30-minute session window starting after login/register
        (function(){
            const body = document.body;
            const isAuth = body.getAttribute('data-auth') === 'True' || body.getAttribute('data-auth') === 'true';
            const userId = body.getAttribute('data-userid');

            // Only track when authenticated and we have a UserId
            if(!isAuth || !userId) {
                // Clear any previous session tracking when not authenticated
                sessionStorage.removeItem('clientSessionStart');
                sessionStorage.removeItem('clientSessionExpiry');
                return;
            }

            const DURATION_MS = 30 * 60 * 1000; // 30 minutes
            let start = Number(sessionStorage.getItem('clientSessionStart'));
            let expiry = Number(sessionStorage.getItem('clientSessionExpiry'));

            if(!start || !expiry) {
                start = Date.now();
                expiry = start + DURATION_MS;
                sessionStorage.setItem('clientSessionStart', String(start));
                sessionStorage.setItem('clientSessionExpiry', String(expiry));
            }

            const check = () => {
                const exp = Number(sessionStorage.getItem('clientSessionExpiry')) || 0;
                if(Date.now() >= exp) {
                    // Clear tracking and force re-login
                    sessionStorage.removeItem('clientSessionStart');
                    sessionStorage.removeItem('clientSessionExpiry');
                    const returnUrl = encodeURIComponent(window.location.pathname + window.location.search);
                    window.location.href = `/Account/Login?client=true&returnUrl=${returnUrl}`;
                }
            };

            // Check every 30s
            setInterval(check, 30 * 1000);
        })();
    </script>
    @RenderSection("Scripts", required: false)
</body>
</html>
