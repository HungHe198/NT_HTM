@model NT.SHARED.Models.Customer

<h2>Customer Details</h2>
<dl class="row">
    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.UserId)</dt>
    <dd class="col-sm-10">@Model.UserId</dd>
    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Address)</dt>
    <dd class="col-sm-10">@Model.Address</dd>
    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.DoB)</dt>
    <dd class="col-sm-10">@Model.DoB</dd>
    <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Gender)</dt>
    <dd class="col-sm-10">@Model.Gender</dd>
</dl>
<p>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</p>
