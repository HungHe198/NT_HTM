@model IEnumerable<NT.SHARED.Models.ProductImage>

@{
    ViewBag.Title = "Qu?n lý ?nh s?n ph?m";
}

<h1>Qu?n lý ?nh s?n ph?m</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">T?o m?i</a>
</p>

<form method="get" asp-action="Index" class="form-inline mb-3">
    <input type="text" name="q" value="" class="form-control mr-2" placeholder="Tìm ki?m theo ???ng d?n..." />
    <button type="submit" class="btn btn-secondary">Tìm ki?m</button>
</form>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Xem tr??c</th>
            <th>???ng d?n ?nh</th>
            <th>Mã bi?n th? SP</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach(var pi in Model)
    {
        <tr>
            <td>
                @if (!string.IsNullOrWhiteSpace(pi.ImageUrl))
                {
                    <img src="@pi.ImageUrl" alt="img" style="max-height:60px; max-width:120px;" />
                }
            </td>
            <td>@pi.ImageUrl</td>
            <td>@pi.ProductDetailId</td>
            <td>
                <a asp-action="Details" asp-route-id="@pi.Id">Chi ti?t</a> |
                <a asp-action="Edit" asp-route-id="@pi.Id">S?a</a> |
                <a asp-action="Delete" asp-route-id="@pi.Id">Xóa</a>
            </td>
        </tr>
    }
    </tbody>
</table>