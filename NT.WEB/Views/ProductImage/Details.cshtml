@model NT.SHARED.Models.ProductImage

@{
    ViewBag.Title = "Chi ti?t ?nh s?n ph?m";
}

<h1>Chi ti?t ?nh s?n ph?m</h1>

<div>
    <h4>?nh</h4>
    @if (!string.IsNullOrWhiteSpace(Model.ImageUrl))
    {
        <img src="@Model.ImageUrl" alt="image" style="max-width:360px; max-height:240px;" />
    }
    <dl class="row mt-3">
        <dt class="col-sm-2">???ng d?n ?nh</dt>
        <dd class="col-sm-10">@Model.ImageUrl</dd>

        <dt class="col-sm-2">Mã bi?n th? SP</dt>
        <dd class="col-sm-10">@Model.ProductDetailId</dd>

        <dt class="col-sm-2">Mã</dt>
        <dd class="col-sm-10">@Model.Id</dd>
    </dl>
</div>

<p>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">S?a</a>
    <a asp-action="Index" class="btn btn-secondary">Quay l?i danh sách</a>
</p>